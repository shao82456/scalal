重点特性
1.多范式的编程语言
    支持非命令式编程及命令式编程如java,c等
    函数式编程
        避免var，和产生size effects(副作用)函数，如没有返回值的函数
    面向对象编程

2.val,var
3.半生对象，半生类
    Apply方法
4.不可变集合和可变集合
    可变的Array(不属于collection)，不可变的List
    可变和不可变的Set,Map
5.Trait
6.类型匹配
7.case类